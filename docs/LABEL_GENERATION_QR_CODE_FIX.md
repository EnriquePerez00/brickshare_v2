# Label Generation QR Code Fix

## Problem

When generating labels for Brickshare PUDO shipments, the system would fail with error:
```
Error al generar etiqueta para [Usuario]: Edge Function returned a non-2xx status code
```

**Root Cause**: The `send-brickshare-qr-email` Edge Function requires the shipment to have a `delivery_qr_code` already generated. If this field is `NULL` or empty, the function returns a 400 error.

## Solution

Updated `LabelGeneration.tsx` to:

1. **Check for QR Code**: Before invoking the Edge Function, check if `delivery_qr_code` exists
2. **Auto-Generate**: If missing, automatically generate the QR code with format `BS-DEL-{first12chars}`
3. **Better Errors**: Provide specific, actionable error messages to operators

### Code Changes

#### generateBrickshareLabel Function
```typescript
const generateBrickshareLabel = async (shipmentId: string) => {
    const shipment = pendingShipments?.find(s => s.id === shipmentId);
    
    // Check if QR code exists, generate if missing
    if (!shipment?.delivery_qr_code) {
        console.log('Delivery QR code missing, generating...');
        const qrCode = `BS-DEL-${shipmentId.substring(0, 12).toUpperCase()}`;
        
        const { error: updateError } = await supabase
            .from('shipments')
            .update({ delivery_qr_code: qrCode })
            .eq('id', shipmentId);
        
        if (updateError) {
            throw new Error(`Error generando código QR: ${updateError.message}`);
        }
        
        console.log('QR code generated:', qrCode);
    }

    // Send QR email to user
    const { data, error } = await supabase.functions.invoke(
        'send-brickshare-qr-email',
        {
            body: {
                shipment_id: shipmentId,
                type: 'delivery'
            }
        }
    );

    if (error) {
        throw new Error(`Error enviando email QR: ${error.message || 'Error desconocido'}`);
    }

    toast.success('Email con QR enviado al usuario');
    return data;
};
```

#### Improved Error Handling
```typescript
onError: (error: Error, shipment) => {
    console.error('Label generation error:', error);
    
    let errorMessage = error.message;
    
    // Provide more specific error messages
    if (errorMessage.includes('PUDO')) {
        errorMessage = `${shipment.users?.full_name} no tiene punto PUDO configurado`;
    } else if (errorMessage.includes('QR')) {
        errorMessage = `Error generando código QR para ${shipment.users?.full_name}`;
    } else if (errorMessage.includes('email')) {
        errorMessage = `Error enviando email a ${shipment.users?.full_name}`;
    } else if (errorMessage.includes('non-2xx')) {
        errorMessage = `Error del servidor al procesar ${shipment.users?.full_name}. Verifica los datos del envío.`;
    }
    
    toast.error(`Error: ${errorMessage}`, {
        description: 'Verifica que el usuario tenga todos los datos necesarios'
    });
}
```

## Why This Happens

The `delivery_qr_code` should normally be generated by the `confirm_assign_sets_to_users()` function when creating the shipment. However, in some edge cases:

1. Manual shipment creation without QR
2. Database migration issues
3. Legacy shipments created before QR implementation

This fix ensures the system is resilient and can recover automatically.

## Testing

### Test Scenario 1: Shipment with Missing QR
1. Create a shipment with `delivery_qr_code = NULL`
2. Go to Operations → Label Generation
3. Click "Generar Etiqueta"
4. **Expected**: QR is auto-generated, email sent successfully

### Test Scenario 2: Shipment with Existing QR
1. Shipment already has `delivery_qr_code`
2. Click "Generar Etiqueta"
3. **Expected**: Uses existing QR, email sent successfully

### Test Scenario 3: Invalid Shipment Data
1. Shipment with missing user email or PUDO
2. Click "Generar Etiqueta"
3. **Expected**: Clear error message indicating what's missing

## Related Files

- `apps/web/src/components/admin/operations/LabelGeneration.tsx` - Main component
- `supabase/functions/send-brickshare-qr-email/index.ts` - Edge Function that validates QR
- `supabase/migrations/20260325133809_make_qr_codes_permanent.sql` - QR code schema

## Migration Note

This fix does **NOT** require database migration or reset. It's a pure frontend fix that handles missing data gracefully.

## Date
25/03/2026 - 18:17 CET